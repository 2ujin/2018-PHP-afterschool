<?php
$uid = setcookie("userid", "");
$name = setcookie("username", "");
$passwd = setcookie("userpasswd", "", time()+10);

if($uid and $name)
    echo "useri, username, userpasswd cookie 생성 완료!";
    ?>